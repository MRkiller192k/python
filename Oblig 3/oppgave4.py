import numpy as np
ganger = int(input ("hvilken verdier vil du ha?"))
returnere_verdien = {}
for i in range(n)
    name, *lengths = input("sett in lengden på du vil ha?").split(" ")

    I_lengths = list(map(float, lengths))
    prism_list[name] = I_lengths
    print(prism_list)
    for n in prism_list:
        print("vilume av en prisme" + n + "er" + str(np.prod(prism_list[n])))